{
  "name": "@wmf/poc-shadow-dom-ssr",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "rimraf ./dist && tsc-watch --onSuccess \"nodemon --delay 0.25 --watch ./dist/ --exec 'node --experimental-modules' ./dist/server.js\"",
    "build": "rimraf ./dist && tsc",
    "start": "node ./dist/server.js",
    "format": "prettier --write \"**/*.{ts,js,html,css}\""
  },
  "license": "MIT",
  "devDependencies": {
    "@types/express": "*",
    "@types/node": "*",
    "express": "*",
    "nodemon": "*",
    "prettier": "*",
    "rimraf": "*",
    "tsc-watch": "*",
    "typescript": "*"
  }
}
